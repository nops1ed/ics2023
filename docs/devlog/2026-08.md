# Development Log - 2026-08

## 2026-08-18 - Recover PAL runtime baseline

Status: In progress  
Author: Maintainer-assisted session

### Goal

Restore the project to a state where its historical RISC-V 64 PAL workload can be built and exercised without course submission side effects.

### Changes

- Selected `extra` as the recovery branch after comparing it with `master` and `pa4`.
- Preserved the removal of tracer-authored compile/run/GDB commits and course submission hooks.
- Restored the public PAL and Navy libc source dependencies through HTTPS.
- Added a Native ELF RV64 NEMU defconfig with device support and tracing disabled.
- Replaced SSH bootstrap URLs with HTTPS and added a clear `PAL_DATA_DIR` boundary for proprietary game resources.

### Design notes

- Game data remains external and ignored; it must come from a licensed PAL copy.
- The first reproducible target is a minimal ramdisk containing PAL and `hello`, matching the current scheduler.
- A containerized Ubuntu toolchain is used because the host lacks the cross compiler, bison/flex, SDL2 headers, and passwordless package installation.

### Verification

- `git diff --stat origin/pa4..origin/extra`: confirmed PAL-relevant loader/mm/VME/device differences.
- `git ls-remote` for `NJU-ProjectN/pal-navy` and `NJU-ProjectN/newlib-navy`: passed.
- Git history, LFS, and unreachable-object checks for PAL data: no resources found.
- Full build and runtime smoke test: in progress.

### Remaining work and risks

- Complete the container build and fix any compiler/runtime regressions.
- A licensed PAL data directory is required for an actual game boot.
- Graphical validation requires a display-capable session.

### Related

- Historical PAL run baseline: `054a4dd3` on `extra`.
